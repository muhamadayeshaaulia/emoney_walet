import 'package:flutter/material.dart';

class AppColors {
  // Warna hijau utama untuk brand E-Money Mamah Saya (E-MS)
  // Menyesuaikan dengan warna hijau pada logo
  static const Color primaryColor = Color(0xFF00A550); // Hijau khas e-money
  
  // Varian warna pendukung
  static const Color primaryDark = Color(0xFF00853E);
  static const Color primaryLight = Color(0xFFE8F6EE);
}
